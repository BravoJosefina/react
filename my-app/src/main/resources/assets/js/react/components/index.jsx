import { App } from "./app";

// Preferably render your react content from index.jsx
// Ideally from one single place
// avoid scattered components entries
ReactDOM.render(<App />, document.getElementById('root'));